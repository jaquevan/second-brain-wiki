handlers:
- match: Questions asked to the user via AskUserQuestion
  patterns:
  - AskUserQuestion
  prompt: 'Answer based on the test case context in input.yaml.

    Default: pick the first option or answer "yes" for confirmations.

    '
